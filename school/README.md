# Система навчання "Школа" 🎓

Система де GPT як вчителька навчає TRM як дитину (аналогія з садочком).

## Концепція

- **GPT Teacher** - вчителька, яка генерує уроки та оцінює відповіді
- **TRM Student** - дитина, яка намагається відповісти на питання
- **School Curriculum** - адаптивна програма навчання (від простого до складного)
- **Kindergarten Learning** - координація всього процесу

## Структура

```
school/
├── __init__.py           # Експорти модулів
├── gpt_teacher.py        # GPT як вчителька
├── trm_student.py        # TRM як дитина
├── curriculum.py         # Програма навчання
└── kindergarten.py       # Головна система
```

## Використання

### Базове використання

```bash
# Запустити навчання на 100 днів
python scripts/school.py --days 100

# З кастомними темами
python scripts/school.py --days 50 --topics-file topics.json

# З іншою GPT моделлю
python scripts/school.py --teacher-model gpt2-medium --device cpu
```

### Параметри

- `--days` - кількість днів навчання (default: 100)
- `--teacher-model` - GPT модель для вчительки (default: "gpt2")
- `--topics-file` - JSON файл з темами (опціонально)
- `--device` - пристрій (cpu/cuda)
- `--config` - конфігурація моделі (default: config/config.yaml)

## Як це працює

1. **Вчителька дає урок** - GPT генерує питання та правильну відповідь
2. **Дитина намагається відповісти** - TRM генерує свою відповідь
3. **Вчителька перевіряє** - GPT оцінює відповідь TRM (cosine similarity)
4. **Адаптація складності** - якщо дитина добре справляється, складність збільшується

## Формат тем (JSON)

```json
[
  {
    "topic": "що таке число",
    "category": "математика",
    "base_difficulty": 1
  },
  {
    "topic": "як працює AI",
    "category": "наука",
    "base_difficulty": 3
  }
]
```

## Прогрес

Прогрес зберігається в `school_progress/progress_day_*.json`:

```json
{
  "day": 10,
  "history": [...],
  "curriculum": {
    "level": 2,
    "lessons_completed": 10,
    "avg_score": 0.75
  }
}
```

## Тестування

```bash
# Перевірити що все працює
python scripts/test_school.py
```

## Приклад виводу

```
[DAY 1] [START] Початок уроку
[TEACHER] Тема: що таке число
[TEACHER] Рівень: 1
[TEACHER] Питання: Простими словами, як для дитини 5 років: що таке число

[STUDENT] Дитина думає...
[STUDENT] Відповідь: Число - це...

[TEACHER] Добре, але можна краще
[TEACHER] Оцінка: 0.70/1.0
```

