# GitHub Repository Setup Guide

## 🚀 Підготовка до експорту на GitHub

Цей документ описує кроки для підготовки проекту до публікації на GitHub.

## 📋 Перевірка готовності

### ✅ Виконано:
- [x] Створено `.gitignore` з усіма необхідними виключеннями
- [x] Підготовлено Google Colab інтеграцію (`colab_setup.ipynb`)
- [x] Створено детальну документацію (`GOOGLE_COLAB_GUIDE.md`)
- [x] Налаштовано конфігурації для Colab
- [x] Очищено код від тимчасових файлів

### 🔄 Перевірте перед експортом:
- [ ] Видалиіть персональні дані з конфігурацій
- [ ] Перевірте шляхи у файлах конфігурації
- [ ] Переконайтеся що всі залежності в `requirements.txt`
- [ ] Перевірте README.md на актуальність

## 🛠️ Кроки експорту

### 1. Ініціалізація Git (якщо ще не зроблено)
```bash
git init
git add .
git commit -m "Initial commit: GPT-2 Two-Phase Training with Colab Support"
```

### 2. Підключення до GitHub
```bash
git remote add origin https://github.com/your-username/your-repo.git
git branch -M main
git push -u origin main
```

### 3. Створення Release
```bash
git tag -a v1.0.0 -m "Initial release with GPT-2 training and Colab support"
git push origin v1.0.0
```

## 📁 Структура репозиторію

```
your-repo/
├── README.md                 # Основна документація
├── requirements.txt          # Залежності Python
├── .gitignore              # Виключення Git
├── colab_setup.ipynb        # Google Colab notebook
├── GOOGLE_COLAB_GUIDE.md    # Інструкція для Colab
├── GITHUB_SETUP.md         # Цей файл
├── config/                 # Конфігурації
│   ├── phase1_pretraining.yaml
│   ├── phase2_instruction_tuning.yaml
│   └── colab_phase2.yaml
├── scripts/                # Скрипти навчання
│   ├── train_phase1_pretraining.py
│   ├── train_phase2_instruction_tuning.py
│   ├── test_model.py
│   └── chat.py
├── datasets/               # Датасети (з gitignore)
├── checkpoints/           # Моделі (з gitignore)
├── logs/                 # Логи (з gitignore)
└── core/                 # Основні моделі
```

## 🔒 Безпека та конфіденційність

### Що НЕ потрапить в Git:
- ✅ Віртуальні середовища (`venv-linux/`)
- ✅ Checkpoints моделей (`checkpoints/`)
- ✅ Логи навчання (`logs/`)
- ✅ Кеш (`cache/`)
- ✅ Тимчасові файли (`*.tmp`, `*.log`)
- ✅ Великі модельні файли (`*.pt`, `*.bin`)
- ✅ Датасети (якщо великі)

### Що ПОТРАПИТЬ в Git:
- ✅ Весь код проекту
- ✅ Конфігурації
- ✅ Документація
- ✅ Notebook для Colab
- ✅ Скрипти для тестування

## 📝 Оновлення README.md

Переконайтеся що README.md містить:

### Обов'язкові секції:
1. **Опис проекту** - що робить цей код
2. **Вимоги** - Python, залежності
3. **Швидкий старт** - як запустити
4. **Структура проекту** - опис папок
5. **Google Colab** - посилання на notebook
6. **Приклади використання** - код прикладів
7. **Ліцензія** - інформація про ліцензію

### Рекомендовані секції:
- **Архітектура** - опис двофазного навчання
- **Конфігурація** - як налаштувати параметри
- **Troubleshooting** - вирішення проблем
- **Contributing** - правила внеску

## 🏷️ Теги та версії

Рекомендована схема версіювання:
- `v1.0.0` - Початковий реліз
- `v1.1.0` - Нові функції
- `v1.0.1` - Багфікс

### Приклади тегів:
```bash
git tag -a v1.0.0 -m "Initial release with GPT-2 two-phase training"
git tag -a v1.0.1 -m "Bug fixes: gradient clipping, label masking"
git tag -a v1.1.0 -m "Add Google Colab support"
```

## 📊 GitHub Pages (опціонально)

Для документації можна налаштувати GitHub Pages:

1. Створіть гілку `gh-pages`
2. Додайте HTML документацію
3. Увімкніть GitHub Pages в налаштуваннях репозиторію

## 🔧 Автоматизація (GitHub Actions)

Можна додати CI/CD:

### .github/workflows/test.yml
```yaml
name: Test
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.8
    - name: Install dependencies
      run: pip install -r requirements.txt
    - name: Run tests
      run: python -m pytest tests/
```

## 🚨 Важливі зауваження

### Перед першим push:
1. **Перевірте `.gitignore`** - переконайтеся що чутливі дані не потраплять в репозиторій
2. **Перевірте README** - має бути зрозумілим для нових користувачів
3. **Перевірте ліцензію** - додайте LICENSE файл
4. **Перевірте залежності** - всі мають бути в `requirements.txt`

### Після публікації:
1. **Додайте description** в GitHub
2. **Додайте topics** для кращої видимості
3. **Напишіть детальний README** якщо ще не готовий
4. **Розгляньте GitHub Pages** для документації

## 📞 Підтримка

Після публікації:
- Відповідайте на issues
- Розглядайте pull requests
- Оновлюйте документацію
- Додавайте приклади використання

## 🎯 Чек-лист перед експортом

- [ ] `.gitignore` налаштований правильно
- [ ] Всі залежності в `requirements.txt`
- [ ] README.md актуальний та зрозумілий
- [ ] Немає персональних даних в конфігураціях
- [ ] Всі шляхи відносні
- [ ] Ліцензія додана
- [ ] Notebook для Colab тестований
- [ ] Документація повна
- [ ] Теги готові до першого релізу

---

**Готово до експорту на GitHub! 🚀**
