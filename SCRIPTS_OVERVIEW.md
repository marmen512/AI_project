# 📋 Огляд всіх скриптів для запуску

## 🚀 Скрипти для запуску навчання

### Linux/macOS

#### 1. `start_training.sh` - Базовий запуск навчання
- **Розташування**: корінь проекту (`./start_training.sh`)
- **Призначення**: Запускає навчання без інтерактивного тестування
- **Використання**:
  ```bash
  chmod +x start_training.sh
  ./start_training.sh
  ```
- **Функції**:
  - Активує venv
  - Перевіряє checkpoint'и для продовження
  - Запускає `runtime.bootstrap`
  - Логує в `logs/training_*.log`

#### 2. `scripts/start_training_with_test.sh` - Навчання з інтерактивним тестуванням ⭐ НОВИЙ
- **Розташування**: `scripts/start_training_with_test.sh`
- **Призначення**: Запускає навчання з можливістю тестування після кожного checkpoint
- **Використання**:
  ```bash
  chmod +x scripts/start_training_with_test.sh
  ./scripts/start_training_with_test.sh
  ```
- **Функції**:
  - Запускає навчання в фоні
  - Моніторить checkpoint'и (перевіряє кожні 5 секунд)
  - Після кожного checkpoint питає: "Протестувати модель? (y/n/f/q)"
  - Timeout: 5 секунд (якщо немає відповіді - продовжує)
  - Опції тестування:
    - `y` - швидкий тест (10 прикладів) з прогрес-баром
    - `f` - повний тест (всі приклади) з прогрес-баром
    - `n`/`q` - пропустити

### Windows

#### 3. `start_training.bat` - Базовий запуск (Command Prompt)
- **Розташування**: корінь проекту (`start_training.bat`)
- **Призначення**: Запускає навчання на Windows через CMD
- **Використання**: Подвійний клік або `start_training.bat` в CMD
- **Функції**: Аналогічно `start_training.sh`

#### 4. `start_training.ps1` - Базовий запуск (PowerShell)
- **Розташування**: корінь проекту (`start_training.ps1`)
- **Призначення**: Запускає навчання на Windows через PowerShell
- **Використання**: `.\\start_training.ps1` в PowerShell
- **Функції**: Аналогічно `start_training.sh`

## 🛠️ Скрипти для налаштування середовища

### Linux/macOS

#### 5. `setup_venv.sh` - Створення venv та встановлення залежностей
- **Розташування**: корінь проекту (`./setup_venv.sh`)
- **Призначення**: Створює віртуальне середовище та встановлює залежності
- **Використання**:
  ```bash
  chmod +x setup_venv.sh
  ./setup_venv.sh
  ```

### Windows

#### 6. `setup_venv.bat` - Створення venv (Command Prompt)
- **Розташування**: корінь проекту (`setup_venv.bat`)
- **Використання**: Подвійний клік або `setup_venv.bat` в CMD

#### 7. `setup_venv.ps1` - Створення venv (PowerShell)
- **Розташування**: корінь проекту (`setup_venv.ps1`)
- **Використання**: `.\\setup_venv.ps1` в PowerShell

## 🧪 Скрипти для тестування

### 8. `scripts/test_model.py` - Повний тест моделі
- **Розташування**: `scripts/test_model.py`
- **Призначення**: Тестує навчену модель на eval датасеті
- **Використання**:
  ```bash
  python scripts/test_model.py --model path/to/model.pt --device cpu
  ```
- **Особливості**:
  - Показує прогрес-бар з відсотками (tqdm)
  - Детальні метрики: точність, схожість, exit_steps

### 9. `train/test_runner.py` - Модуль для швидкого тестування ⭐ НОВИЙ
- **Розташування**: `train/test_runner.py`
- **Призначення**: Модуль для швидкого тестування з прогрес-баром
- **Функції**:
  - `quick_test_model()` - швидкий тест на N прикладах
  - `full_test_model()` - повний тест через `test_model.py`
  - `print_test_results()` - виведення результатів
- **Використання**:
  ```python
  from train.test_runner import quick_test_model, print_test_results
  
  results = quick_test_model(
      model_path="checkpoints/checkpoint_latest.pt",
      num_samples=10,
      device="cpu",
      show_progress=True
  )
  print_test_results(results, 'quick')
  ```

## 📊 Інші корисні скрипти

### 10. `scripts/school.py` - Система навчання "Школа"
- **Призначення**: GPT вчителька навчає TRM дитину
- **Використання**: `python scripts/school.py --days 100`

### 11. `scripts/test_school.py` - Тестування системи "Школа"
- **Призначення**: Тестує компоненти системи "Школа"
- **Використання**: `python scripts/test_school.py`

## 📝 Підсумок

### Для Linux/macOS:
- **Базове навчання**: `./start_training.sh`
- **Навчання з тестуванням**: `./scripts/start_training_with_test.sh` ⭐ РЕКОМЕНДОВАНО
- **Налаштування**: `./setup_venv.sh`

### Для Windows:
- **Базове навчання**: `start_training.bat` (CMD) або `.\start_training.ps1` (PowerShell)
- **Налаштування**: `setup_venv.bat` (CMD) або `.\setup_venv.ps1` (PowerShell)
- **Примітка**: Скрипт з інтерактивним тестуванням поки тільки для Linux

## ✅ Перевірка коректності

Всі скрипти перевірені:
- ✅ `scripts/start_training_with_test.sh` - коректно інтегровано
- ✅ `train/test_runner.py` - правильно використовує `load_trained_model`
- ✅ `scripts/test_model.py` - оновлено з прогрес-баром
- ✅ Всі імпорти коректні
- ✅ Немає синтаксичних помилок

## 🔍 Де знаходяться скрипти

```
G:\2\
├── start_training.sh          # Linux: базовий запуск
├── start_training.bat          # Windows CMD: базовий запуск
├── start_training.ps1          # Windows PowerShell: базовий запуск
├── setup_venv.sh               # Linux: налаштування
├── setup_venv.bat              # Windows CMD: налаштування
├── setup_venv.ps1              # Windows PowerShell: налаштування
└── scripts/
    ├── start_training_with_test.sh  # Linux: навчання з тестуванням ⭐
    ├── test_model.py                 # Повний тест моделі
    ├── school.py                     # Система "Школа"
    └── test_school.py                 # Тест системи "Школа"
└── train/
    └── test_runner.py                # Модуль швидкого тестування ⭐
```

